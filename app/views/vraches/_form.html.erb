<%= form_with(model: vrach) do |form| %>
  <% if vrach.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vrach.errors.count, "error") %> prohibited this vrach from being saved:</h2>

      <ul>
        <% vrach.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
