<%= form_with(model: klinika) do |form| %>
  <% if klinika.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(klinika.errors.count, "error") %> prohibited this klinika from being saved:</h2>

      <ul>
        <% klinika.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
