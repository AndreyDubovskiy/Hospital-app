<h1>New Pacient</h1>
<%= form_for @pacient do |form| %>
  <% if @pacient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pacient.errors.count, "error") %> prohibited this pacient from being saved:</h2>
      <ul>
        <% @pacient.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :card_pacient_id %>
    <%= form.collection_select :card_pacient_id, CardPacient.all, :id, :adress, { prompt: 'Выберите card_pacient' } %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>